package com.payment_wallet.notification_service.entity;

/** Category of a notification, used by the UI to choose an icon/label. */
public enum NotificationType {
    TRANSFER_SENT,
    TRANSFER_RECEIVED,
    FUNDS_ADDED,
    REWARD_EARNED,
    GENERAL
}
